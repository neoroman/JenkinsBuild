// Marker for dist.sh / Gradle discovery (minimal fixture; not a full app module).
apply plugin: 'com.android.application'

android {
    compileSdk versions.compileSdk

    defaultConfig {
        minSdk versions.minSdk
        targetSdk versions.compileSdk
        versionCode 144
        versionName "2.0.4"
    }
}
